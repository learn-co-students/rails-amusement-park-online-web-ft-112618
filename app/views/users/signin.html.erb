<%= form_tag '/login' do %>
  <%= collection_select(:user, :name, User.all, :name, :name) %>
  <%= label_tag :password %>
  <%= password_field_tag :password %>
  <%= submit_tag "Sign In" %>
<% end %>
